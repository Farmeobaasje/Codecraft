<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- TODO: Include only the files that need to be backed up. -->
        <include domain="file" path="." />
        <exclude domain="file" path="no_backup/" />
    </cloud-backup>
    <!--
    <device-transfer>
        <include domain="file" path="." />
        <exclude domain="file" path="no_transfer/" />
    </device-transfer>
    -->
</data-extraction-rules>
